# 测试用例管理模块 · 产品设计方案（PRD）

> **演进目标**：把现有「基础用例库（`basicCaseLibrary`）」升级为企业级**测试用例管理模块**，对标阿里云效（用例库 + 测试计划 + 测试执行）、腾讯 TAPD（用例库 + 用例评审 + 测试周期）的成熟范式。  
> **设计原则**：用例资产（库）与执行实例（计划）解耦；先打通闭环主干，再补高级能力。



---

## 0. 现状盘点（Grounding）

### 0.1 已有的能力（不要重复造轮子）

| 维度  | 现状                                                                                                                                                                   |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 组织  | 业务线 `business`（本地收款/本地付款/卡收单/代付/VA…）→ 分类 `category` 两级目录树，支持折叠                                                                                                       |
| 字段  | 标题、测试目标 `objective`、前置条件、测试数据 `testData`、步骤 `steps`、预期 `expected`、优先级 P0–P3、组件 `component`、标签 `tags`、关联测试计划 `testPlans`、执行历史 `executionHistory`、关联缺陷 `linkedDefects` |
| 状态机 | 草稿 → 待评审 → 已确认 → 已废弃                                                                                                                                                 |
| 操作  | 新增/编辑/复制/删除、批量操作、搜索、按优先级/状态筛选、排序、激活筛选标签吸顶                                                                                                                            |

### 0.2 对标阿里/腾讯的缺口

1. **目录仅两级**，无法支撑「产品 → 模块 → 子功能 → 场景」多级管理。
2. **用例类型缺失**：没有功能/接口/性能/安全/兼容/UI 分类，无法针对性统计。
3. **没有测试计划编排**：库里的 `testPlans` 只是字符串数组，无法真正把用例编入某迭代并指派执行人。
4. **执行流不完整**：`executionHistory` 是散记录，没有「轮次 / 执行人 / 结果(通过·失败·阻塞·跳过) / 关联缺陷」的结构化执行实例。
5. **评审是状态不是流程**：没有评审单、评审意见、评审结论留痕。
6. **无全链路追溯**：用例 ↔ 需求 ↔ 缺陷 ↔ 自动化脚本没有关联视图。
7. **无覆盖率/进度看板**：无法看「需求覆盖率、计划执行率、缺陷拦截率」。
8. **无导入导出**：不支持 Excel / XMind 批量维护（阿里云效、TAPD 都支持）。
9. **资产与执行混淆**：`cases`（绑定 taskId）和 `basicCaseLibrary`（资产）两套数据并存，易脏。

---

## 1. 问题陈述（Problem Statement）

当前「基础用例库」还停留在「用例文档收纳」阶段。QA 在真实项目里需要的是**可追溯、可编排、可执行、可度量**的用例管理：用例要能按迭代编成测试计划、分给具体人执行、记录每轮结果、一键关联缺陷、回链到需求，并通过看板让管理者看到覆盖率和进度。缺这套闭环，用例库就是「写完没人维护、用的时候找不到、复盘时没有数据」的死库。

- **受影响角色**：测试负责人（编排/度量）、测试工程师（执行/记录）、开发（看覆盖率/收缺陷）、PM/管理者（看进度与质量）。
- **不做的代价**：用例资产折旧快、回归遗漏、缺陷漏测、发布质量不可量化，在竞品（云效/TAPD）面前丧失说服力。

---

## 2. 目标（Goals）

| #  | 目标                        | 衡量方式                       |
| -- | ------------------------- | -------------------------- |
| G1 | 用例资产可多级组织、可检索、可复用         | 90% 用例落在三级以上目录；搜索命中率 > 95% |
| G2 | 用例能「一键编入测试计划 + 指派执行人」     | 测试计划创建到首条用例入计划 < 3 分钟      |
| G3 | 执行结果结构化回写，支持多轮            | 执行结果字段完整率 100%（无空结果）       |
| G4 | 用例 ↔ 需求 ↔ 缺陷 ↔ 自动化 全链路可追溯 | 关联缺陷的用例占比可统计；覆盖率看板可用       |
| G5 | 管理者能看覆盖率/进度/缺陷看板          | 看板周活（管理者角色）> 60%           |

---

## 3. 非目标（Non-Goals）

1. **不做独立的需求管理/缺陷管理**：需求、缺陷沿用现有 `batches`/`bugs`（只做关联，不做第二套）。
2. **不做自动化执行引擎**：自动化脚本执行复用现有 `automationSteps` + playwright 能力，本模块只做「关联与结果回填」，不重造执行器。
3. **不做多人实时协同编辑**：见 §10 风险，v1 维持整包状态覆盖模型，不做 OT/CRDT 实时协同。
4. **不做跨项目用例市场/模板商城**：v1 不做组织级用例共享市场。
5. **不替代 Excel 做复杂用例编写**：导入导出只是双向同步手段，不是主编辑界面。

---

## 4. 信息架构 / 模块地图

```
测试用例管理（一级 Tab：沿用现有 basicCases）
├── 用例库（资产）          ← 演进现有 basicCaseLibrary
│   ├── 多级目录树（业务线 → 产品 → 模块 → 场景）
│   ├── 用例列表（搜索/筛选/排序/批量）
│   └── 用例详情/编辑（抽屉式）
├── 测试计划（编排）        ← 新增
│   ├── 计划列表（按 batch/迭代）
│   ├── 计划详情（引用用例、指派执行人、轮次）
│   └── 执行看板
├── 测试执行（运行）        ← 新增（结构化结果）
├── 用例评审（流程）        ← 新增（评审单）
├── 追溯视图（图谱）        ← 新增（用例↔需求↔缺陷↔自动化）
└── 统计看板（度量）        ← 新增（覆盖率/进度/缺陷）
```

> 入口策略：在现有 `basicCases` 内用子 Tab 切换「库 / 计划 / 执行 / 评审 / 看板」，避免新增顶层导航割裂。

---

## 5. 演进后的用例数据模型

> `basicCaseLibrary` 中每条用例 = **资产（Asset）**，与执行解耦。下列 `★` 为新增字段。

```js
{
  id,                 // 现有
  business,           // 现有：业务线
  product,            // ★ 产品（如「收款核心」「付款核心」）
  module,             // ★ 模块（如「入账」「幂等」），替代/细化原 category
  category,           // 现有：保留为「场景标签」或迁移为 module
  title,              // 现有
  type,               // ★ 用例类型：功能|接口|性能|安全|兼容|UI|其他
  objective,          // 现有：测试目标
  preconditions,      // 现有
  testData,           // 现有
  steps,              // 现有（建议升级为结构化步骤数组，见 §6.2）
  expected,           // 现有
  priority,           // 现有：P0–P3
  status,             // 现有：草稿/待评审/已确认/已废弃
  component,          // 现有
  tags,               // 现有
  automationSteps,    // 现有（来自 AI 生成）：关联自动化脚本
  automationEnabled,  // 现有
  linkedRequirements, // ★ 关联需求 id（来自 batches/tasks）
  linkedDefects,      // 现有：关联缺陷
  reviewId,           // ★ 当前评审单 id（见 §6.3）
  version,            // ★ 资产版本号（每次编辑+1，支持基线）
  createdBy/updatedBy/createdAt/updatedAt, // 现有
}
```

**执行实例（Execution Instance）**——新增独立集合 `caseExecutions`，引用资产但不改资产：

```js
{
  id, caseAssetId, planId, round,        // 第几轮
  executor, status,                       // 未执行/通过/失败/阻塞/跳过
  startedAt, finishedAt,
  resultNote, linkedDefectId, evidence,  // 截图/日志
}
```

---

## 6. 核心子模块设计

### 6.1 用例库与多级目录

- **结构**：业务线 → 产品 → 模块 → 场景（最多四级，可折叠）。沿用现有 `business` 一级，新增 `product/module` 两级。
- **对标**：阿里云效的「用例库目录树」、TAPD 的「用例文件夹」，均支持多级。
- **关键规则**：目录可拖拽调整归属；重命名目录级联更新用例归属；删除目录需先清空/迁移用例（防误删）。
- **检索**：支持标题/步骤/标签/组件全文搜；筛选维度 = 类型、优先级、状态、组件、标签、是否自动化。

### 6.2 用例编辑与字段

- **结构化步骤**：把 `steps` 纯文本升级为步骤数组 `[{action, expected, data}]`，与现有 `automationSteps` 一一映射（AI 生成时已输出 automationSteps，可反推步骤骨架）。
- **用例类型**：新增 `type`（功能/接口/性能/安全/兼容/UI），驱动统计与筛选。
- **模板**：提供「功能用例模板 / 接口用例模板（请求-响应）/ 异常用例模板」，降低编写成本（对标云效的用例模板）。
- **版本与基线**：每次保存 `version+1`，可查看历史版本、可「另存为基线」供回归引用。

### 6.3 用例评审（Review）

- **评审单（Review Ticket）**：从「待评审」用例批量发起评审单，指定评审人、截止时间。
- **评审动作**：通过 / 打回（附意见）/ 需修改；结论写回用例 `status`（待评审→已确认/草稿）并留痕 `reviewId`。
- **对标**：云效「用例评审」、TAPD「用例评审单」——用轻量评审单替代 email/IM 扯皮。

### 6.4 测试计划（Test Plan）

- **编排**：选择某 `batch`（迭代/版本）创建计划 → 从用例库「引用」用例（引用而非复制，保留资产单一来源）→ 指派每条/每批用例的执行人。
- **轮次**：计划支持多轮执行（如回归轮、复测轮），每轮生成 `caseExecutions`。
- **对标**：云效「测试计划把用例加入并指派」、TAPD「测试周期」。

### 6.5 测试执行（Execution）

- **执行台**：按执行人/计划维度看待办；逐条标记 通过/失败/阻塞/跳过，失败可「一键建 Bug」并回填 `linkedDefectId`。
- **证据**：支持上传截图/日志到 `evidence`。
- **回写**：执行结果汇总到计划进度；可选地回写资产 `executionHistory`（仅汇总，不污染资产字段）。
- **关键约束**：执行实例写入 `caseExecutions`，**不修改 `basicCaseLibrary` 资产**——避免现有 `mergeCasesIntoState` 整体替换冲掉手动补录的坑。

### 6.6 关联与追溯

- **需求**：用例可关联 `batches`/`tasks`（需求）id，`linkedRequirements`。
- **缺陷**：执行失败时关联/创建 `bugs`，`linkedDefects`。
- **自动化**：展示 `automationEnabled` 脚本与最近运行结果。
- **追溯视图**：图谱/列表展示「需求 → 用例 → 执行 → 缺陷」链路，支持反向钻取。

### 6.7 统计看板

| 指标    | 定义              | 对标        |
| ----- | --------------- | --------- |
| 需求覆盖率 | 已有关联用例的需求 / 总需求 | 云效覆盖率     |
| 计划执行率 | 已执行用例 / 计划用例    | TAPD 执行进度 |
| 通过率   | 通过 / 已执行        | 通用        |
| 缺陷拦截率 | 执行发现并建单的缺陷数     | 质量度量      |
| 自动化占比 | 已关联自动化脚本的用例占比   | 云效自动化     |

### 6.8 导入导出

- **导出**：选中用例/整目录导出 Excel（云效/TAPD 标准列）与 XMind（树状）。
- **导入**：Excel 按模板回灌，做字段映射与冲突校验（重名/缺字段告警）。
- **复用**：已有 python-docx 导出链路可扩展为 xlsx 导出。

### 6.9 自动化集成

- 直接复用现有 `automationSteps` + playwright 执行；本模块增加「在用例详情页触发执行 / 展示最近结果 / 失败自动建议建 Bug」。

---

## 7. 用户故事（MoSCoW）

**P0（Must）**

- 作为测试工程师，我想把用例按「业务线→产品→模块」多级归档，以便结构化管理资产。
- 作为测试负责人，我想从用例库引用用例创建测试计划并指派执行人，以便组织一轮测试。
- 作为测试工程师，我想在执行台逐条标记结果并一键建 Bug，以便记录执行。
- 作为测试工程师，我想设置用例类型为功能/接口/性能等，以便分类统计。
- 作为任何角色，我想搜索/筛选用例（类型/优先级/状态/标签），以便快速找到。

**P1（Should）**

- 作为测试负责人，我想发起用例评审单并留痕结论，以便质量控制。
- 作为管理者，我想看覆盖率/执行率/缺陷看板，以便掌握进度。
- 作为测试工程师，我想导出 Excel / XMind 并与同事同步，以便离线维护。
- 作为任何角色，我想查看用例版本历史与基线，以便回归引用。

**P2（Could / Future）**

- 作为测试工程师，我想用结构化步骤编辑器编写用例，并与自动化脚本联动。
- 作为管理者，我想看用例-需求-缺陷追溯图谱。
- 支持批量 AI 补全步骤/预期。

---

## 8. 优先级与分期（Phasing）

| 期次                | 范围                                           | 产出      |
| ----------------- | -------------------------------------------- | ------- |
| **P0 · 闭环主干（v1）** | 多级目录、用例类型字段、测试计划引用+指派、执行台+结果回写+建 Bug、基础筛选搜索  | 可用的最小闭环 |
| **P1 · 质量与度量**    | 用例评审单、统计看板（覆盖率/执行率/缺陷）、Excel/XMind 导入导出、版本历史 | 可度量、可协同 |
| **P2 · 增强**       | 结构化步骤编辑器、追溯图谱、AI 批量补全、自动化结果联动面板              | 体验与智能化  |

> 建议先落地 P0 打通「库→计划→执行」，让用例真正跑起来，再补度量。

---

## 9. 指标体系（Metrics）

- **采纳（领先）**：P0 上线 30 天内，活跃用例库用户占比 ≥ 70%；计划创建数周环比上升。
- **激活（领先）**：执行台周活 ≥ 测试工程师的 80%；执行结果完整率 100%。
- **留存/质量（滞后）**：需求覆盖率逐迭代 ≥ 60%；执行发现的缺陷占迭代总缺陷 ≥ 40%（测试前置价值）。
- **满意度**：NPS 较「纯文档库」阶段 +10。

---

## 10. 风险与约束

| 风险                                                                        | 影响               | 缓解                                                               |
| ------------------------------------------------------------------------- | ---------------- | ---------------------------------------------------------------- |
| **整包状态覆盖（已知架构债）**：`persist()` 整包 POST `SHARED_STATE_KEYS`，无乐观锁，多人同时用会互相覆盖 | 多 QA 并发编辑用例库会丢数据 | v1 明确「单写者/串行」约定；P1 引入版本号+冲突检测（ETag/`_rev`），或拆分 REST 资源端点         |
| **资产/执行混淆**：现有 `cases`(taskId 绑定) 与 `basicCaseLibrary` 并存                 | 数据脏、重复维护         | 明确 `basicCaseLibrary`=资产，`caseExecutions`=执行实例，旧 `cases` 逐步迁移/只读 |
| **AI 重生成冲掉手补用例**：`mergeCasesIntoState` 按任务整体替换                            | 手动补录丢失           | 执行写 `caseExecutions`，不回写资产；AI 生成仅建议，确认后入资产                       |
| **前端单文件过大**：`app.js` 已 9000+ 行                                            | 维护成本、回归风险        | 新模块建议按子 Tab 拆分视图模块，避免继续堆在 `app.js`                               |
| **隐私合规**：用例含业务测试数据，可能涉及敏感信息                                               | 合规风险             | 导入导出/截图脱敏，遵循现有个人信息保护约定                                           |

---

## 11. 开放问题（Open Questions）

1. **目录深度**：业务线→产品→模块→场景 四级是否过深？是否允许自定义层级？（待 PM 拍板）
2. **`cases` 存量**：旧版本绑定用例是直接迁移到 `basicCaseLibrary` 还是保留两套？（待架构）
3. **执行人来源**：执行人是否直接复用 `team-members.json`？是否需要执行排班？（待 PM）
4. **多轮执行**：复测轮是否复用首轮用例集还是可增删？（待 QA 负责人）
5. **看板数据窗口**：覆盖率按「当前迭代」还是「滚动 30 天」？（待管理者）

---

## 附：与阿里/腾讯的能力对齐表

| 能力               | 阿里云效  | 腾讯 TAPD | 本方案             |
| ---------------- | ----- | ------- | --------------- |
| 多级用例目录           | ✅     | ✅       | ✅ P0            |
| 用例类型/优先级         | ✅     | ✅       | ✅ P0            |
| 用例评审单            | ✅     | ✅       | ✅ P1            |
| 测试计划/周期          | ✅     | ✅       | ✅ P0            |
| 测试执行/结果          | ✅     | ✅       | ✅ P0            |
| 需求/缺陷关联          | ✅     | ✅       | ✅ P0/P1         |
| 覆盖率/进度看板         | ✅     | ✅       | ✅ P1            |
| Excel/XMind 导入导出 | ✅     | ✅       | ✅ P1            |
| 自动化用例关联          | ✅     | ✅       | ✅ 复用现有          |
| 实时协同编辑           | ✅(部分) | ❌       | ❌ v1（架构债，见 §10） |
